export enum StepFlow {
    PassengerInformation,
    TicketInformation,
    OrderSummary
}

export enum Sex {
    Male=1,
    Female=2
}

export enum TicketClass {
    Economy,
    Business,
}

export enum TicketType {
    OneWay,
    RoundTrip
}
